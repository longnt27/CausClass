# Import provenance

Source repository: https://github.com/longnt27/CausClass-report

Source commit: `947714f3d1eb517c19e697f394763b1f7e8e9944` (initial report snapshot).

Source tree: `c25485c8cb4f93fffa6cd93cf3f65eb5730088a3`.

Destination prefix: `report/` in https://github.com/longnt27/CausClass.

Import date: 2026-09-07. The report commit was fetched, joined as a second parent
and read under the report/ prefix. Its original history and authorship remain in
the Git graph; this is not a submodule and a fresh clone needs no second checkout.

Removed from the working tree only: `.DS_Store` and generated main.aux,
main.fdb_latexmk, main.fls, main.lof, main.log, main.lot, main.out and main.toc.
These remain recoverable from the source commit. No report sections, numerical
results, bibliography entries, appendices or figures were removed.

The import is a pinned snapshot, not a live synchronization. Future writing should
use this repository as the code/report source of truth. The separate report repo
is unchanged; archiving it is a maintainer decision. No additional copyright or
data permissions are inferred; see ../THIRD_PARTY_NOTICES.md.
